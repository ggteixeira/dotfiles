[core]
	editor = nvim

[gpg]
	program = gpg
	format = ssh

[commit]
	gpgsign = true

[tag]
	forceSignAnnotated = false

[pull]
	rebase = false

[init]
	defaultBranch = main

[user]
  signingkey = ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIC90FUy30LfAm5CA2bd22htCLRErYQP5cklT/dmht6Kn
  username = ggteixeira
	name = Guilherme Teixeira
  email = 24235344+ggteixeira@users.noreply.github.com

[push]
	default = current

[alias]
	l = "log --graph --pretty=format:'%C(yellow)%h%Creset -%C(red)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit"
	st = status
	com = commit
	diffcom = git diff HEAD~1
	git = !git
  unstage = restore --staged . 
  adp = add -p

[gpg]
	program = gpg 

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
